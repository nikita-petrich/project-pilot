.git
.github
.claude
compose*.yaml
deploy
.venv
__pycache__
*.py[cod]
.mypy_cache
.ruff_cache
.pytest_cache
.coverage
.coverage.*
htmlcov
dist
build
.env
.env.*
tests
blueprint
docs
prototypes
*.md
!README.md
# The public profile comes from the website at runtime; what still has to ride
# inside the image is the half that cannot be published. Named explicitly because
# losing it to an ignore rule fails the container at boot, not at build time.
!profile/private.yaml
media
