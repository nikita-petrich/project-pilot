# Copy to .env next to compose.yaml on the server. One value, no secrets —
# the hostnames themselves live in the Caddyfile, one block per MCP server.

# Where Let's Encrypt sends certificate-expiry warnings.
ACME_EMAIL=you@example.com
